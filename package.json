{
  "name": "lambda-npm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "npm install && npm run compile && zip -r deploy.zip ./",
    "clean": "rm index.js index.js.map tracing.js tracing.js.map && rm -rf node_modules && rm deploy.zip",
    "upload": "aws lambda update-function-code --function-name hello-world --zip-file fileb://deploy.zip",
    "aws": "npm run clean && npm run zip && npm run upload"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@opentelemetry/core": "^0.12.0",
    "@opentelemetry/exporter-collector": "^0.12.0",
    "@opentelemetry/node": "^0.12.0",
    "@opentelemetry/plugin-http": "^0.12.0",
    "@opentelemetry/tracing": "^0.12.0",
    "@types/aws-lambda": "^8.10.51",
    "@types/node": "^13.13.5",
    "aws-sdk": "^2.655.0",
    "axios": "^0.21.0",
    "lightstep-opentelemetry-launcher-node": "^0.11.0",
    "typescript": "^3.8.3",
    "uuid": "^8.3.1"
  }
}
